# Verifies the production Docker image builds successfully.
# Runs on PRs touching Docker-related files and on main/v0 pushes.
name: Docker Build

# Least-privilege: no default token permissions granted. This build only needs
# to read the repo and push to the GHA cache — no writes to the repo/registry.
permissions:
  contents: read

on:
  push:
    branches: [main, v0]
    paths:
      - "Dockerfile"
      - "Dockerfile.worker"
      - "docker-compose.yml"
      - ".dockerignore"
      - "docker-entrypoint.sh"
      - "package.json"
      - "package-lock.json"
  pull_request:
    paths:
      - "Dockerfile"
      - "Dockerfile.worker"
      - "docker-compose.yml"
      - ".dockerignore"
      - "docker-entrypoint.sh"
      - "package.json"
      - "package-lock.json"

jobs:
  build-app:
    name: Build app image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build app image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          tags: lemniscate:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Inspect image
        run: docker image inspect lemniscate:ci --format='{{.Os}}/{{.Architecture}} size={{.Size}}'

  build-worker:
    name: Build worker image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build worker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.worker
          push: false
          load: true
          tags: lemniscate-worker:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
